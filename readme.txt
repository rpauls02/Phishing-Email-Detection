Included in the submission folder is
1) Optimised dataset used for creating the test set
2) Word embeddings generated by CBOW used for testing primary model

Running the code cell under the 'Primary Models' section will generate and apply the sets for testing

1. Run code in 'Load Packages' and 'Mount Google Drive' sections

2. For baseline models:
    1. Run code in 'Tokenisation' section
    2. Run code in 'Baseline Models' section

3. For primary models:
    1. Run code in 'Primary Models' section

